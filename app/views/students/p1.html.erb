<%= render "shared/main_nav", back_path: students_path, title: "Participant Profile" %>

<section>
  <%= image_tag student.image, size: "100x100" %>
  <h2><%= student.full_name %></h2>
  <p>Age <%= student.age %></p>
  <p>Program participant since <%= student.joined_at %></p>
  <%= link_to "New 1 on 1", new_interaction_path %>
  <!-- TODO replace with real chart -->
  <div id="interaction-chart" data-id="<%= student.id %>">Chart placeholder</div>
</section>
